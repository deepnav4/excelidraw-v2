{
  "buildCommand": "cd apps/web && npx turbo build",
  "outputDirectory": ".next",
  "installCommand": "npm install -g pnpm && pnpm install --frozen-lockfile",
  "framework": null,
  "ignoreCommand": "git diff --quiet 'HEAD^' HEAD ./apps/web 2>/dev/null || exit 1"
}
